{
  "name": "don-rashid-site",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node --check script.js && node --check game/game.js && node --check arcade/core.js && node --check arcade/arcade.js && node --test tests/*.test.mjs"
  }
}
